def calcA(base, altura):
    res = base*altura/2
    return(res)

base = float(input("Digite a base em cm: "))
altura = float(input("Digite a altura em cm: "))

print("A área é:", calcA(base, altura))
    
